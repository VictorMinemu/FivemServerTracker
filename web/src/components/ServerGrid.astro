---
/**
 * Responsive grid container for server cards.
 * Adjusts columns to account for the sidebar on larger screens.
 * 1 col mobile, 2 on sm+, 3 on xl+ (where sidebar + 3 cols fit).
 */
---

<div class="grid grid-cols-1 gap-4 sm:grid-cols-2 xl:grid-cols-3">
  <slot />
</div>
