/**
 * FiveM Server Tracker - Entry Point
 *
 * Initializes the database connection and starts the polling scheduler
 * that fetches server data from FiveM's protobuf stream endpoint.
 *
 * This file will be populated in Plan 01-04 when the polling pipeline
 * is integrated with the cron scheduler.
 */

export {};
